<% if notice && notice.is_a?(Array) %>
    <%= notice.each do |n| %>
        <p class="alert alert-success" role="alert">
            <%= n %>
        </p>
    <% end %>
<% elsif notice && notice.is_a?(String) %>
    <p class="alert alert-success" role="alert">
        <%= notice %>
    </p>
<% end %>

<% if alert && alert.is_a?(Array) %>
    <%= alert.each do |a| %>
        <p class="alert alert-success" role="alert">
            <%= a %>
        </p>
    <% end %>
<% elsif alert && alert.is_a?(String) %>
    <p class="alert alert-success" role="alert">
        <%= alert %>
    </p>
<% end %>